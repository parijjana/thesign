org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin incremental compilation breaks when the pub cache (C:) and the project
# (D:) live on different drive roots — its relocatable cache can't compute a
# cross-root relative path (IllegalArgumentException: different roots). Plain
# (non-incremental) compilation sidesteps it. See ROADMAP M5 notes.
kotlin.incremental=false
